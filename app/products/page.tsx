import Link from "next/link"; import Image from "next/image"; import { products } from "@/src/data/catalog";
export const metadata = { title:"Products", description:"Handcrafted SERVOMUTO lighting collections." };
export default function Products(){return <main className="listing"><header><p className="index">ALL PRODUCTS</p><h1>Objects of <em>light.</em></h1></header><nav className="filters" aria-label="Product categories">{["All","Pendant","Table","Wall","Floor"].map(x=><Link key={x} href={x==="All"?"/products":`/products?type=${x.toLowerCase()}`}>{x}</Link>)}</nav><div className="listing-grid">{products.map(product=><Link key={product.slug} href={`/products/${product.slug}`} className="listing-card"><div><Image src={product.images[0]} alt={product.name} fill sizes="(max-width:700px) 100vw, 33vw" className="contain" /></div><p>{product.type} · {product.collection}</p><h2>{product.name}</h2><span>View product →</span></Link>)}</div></main>}
